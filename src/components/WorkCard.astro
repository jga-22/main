---
const { title, period, summary, tags } = Astro.props;
---

<article class="card card--work">
  <div class="card__meta">{period}</div>
  <h3>{title}</h3>
  <p>{summary}</p>
  <ul class="tag-list" aria-label={`${title} tags`}>
    {tags.map((tag) => <li>{tag}</li>)}
  </ul>
</article>
